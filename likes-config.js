window.SFF_SUPPORTERS = {
  // One-time setup on github.com (clicks only — no scripts):
  //
  // EASIEST: Classic token
  //   Settings → Developer settings → Personal access tokens → Tokens (classic)
  //   → Generate new token → check "public_repo" → Generate
  //
  // OR fine-grained token:
  //   Settings → Developer settings → Fine-grained tokens → Generate
  //   → Repository access: StressFreeFlowSite only
  //   → Repository permissions → Contents → Access: Read and write
  //   (Metadata read is added automatically)
  //
  // Paste the token below, then push this file once.
  dispatchToken: "",
};
